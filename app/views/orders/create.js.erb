// html will work even without this page
$('.clear-order').val('');
$('.modal').modal('hide');
var app_order = $("<%= j render 'orders/order_receipt', order: @order %>");
$('#order').prepend(app_order);
// ^ append is working on the lounges/show page, i needed to append to the id order

